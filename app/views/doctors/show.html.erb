<h1><%= @doctor.name %></h1>
<h3><%= @doctor.department %></h3>

<% @doctor.appointments.each do |app| %>
  <p>
    <%= app.formatted_datetime %> <%= link_to app.patient.name, patient_path(app.patient) %>
  </p>
<% end %>
