<h1><%= @doctor.name %>'s Appointments</h1>

<ul>
    <%= @doctor.appointments.each do |a| %>
    <li><%= a.date_time %><%= link_to a.patient_name, patient_path(a.patient) %></li>
    <% end %>
</ul>
