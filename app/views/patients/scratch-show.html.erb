<%= @patient.name %>
<br>
<%= @patient.appointments.each do |a| %>
  <%= link_to a.doctor.name, doctor_path(a) %> - <%= link_to a.appointment_datetime.strftime("%B %d, %Y at %k:%M"), appointment_path(a) %>
<% end %>
